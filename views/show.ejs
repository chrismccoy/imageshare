<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/_head') %>
    <title><%= image.key %> — Image Share</title>
  </head>
  <body class="bg-surface-50 text-surface-700 h-screen flex flex-col antialiased">

    <header class="flex items-center justify-between px-5 py-3 border-b border-surface-200 bg-white flex-shrink-0 shadow-sm">
      <div class="flex items-center gap-4 text-sm">
        <a href="/" class="inline-flex items-center gap-1.5 font-semibold text-brand-600 hover:text-brand-700 transition">
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" d="M10.5 19.5 3 12m0 0 7.5-7.5M3 12h18" />
          </svg>
          New
        </a>
        <div class="w-px h-4 bg-surface-200"></div>
        <span class="text-surface-400"><%= image.dimensionsText %></span>
        <span class="text-surface-400"><%= image.sizeText %></span>
        <span class="text-surface-400"><%= image.ttlText %> TTL</span>
      </div>
      <div class="flex items-center gap-1">
        <a
          href="<%= image.fileUrl %>"
          target="_blank"
          rel="noopener"
          class="inline-flex items-center gap-1.5 px-3 py-1.5 text-sm font-medium rounded-lg text-surface-500 hover:text-surface-700 hover:bg-surface-100 transition"
        >
          <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 6H5.25A2.25 2.25 0 0 0 3 8.25v10.5A2.25 2.25 0 0 0 5.25 21h10.5A2.25 2.25 0 0 0 18 18.75V10.5m-10.5 6L21 3m0 0h-5.25M21 3v5.25" />
          </svg>
          Open
        </a>
      </div>
    </header>

    <main class="flex-1 min-h-0 flex items-center justify-center p-6">
      <img
        src="<%= image.fileUrl %>"
        alt="Shared image <%= image.key %>"
        width="<%= image.width %>"
        height="<%= image.height %>"
        class="max-w-full max-h-full object-contain rounded-lg shadow-xl shadow-surface-200/50 bg-white"
      />
    </main>

  </body>
</html>
